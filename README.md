# CJFace Discord Bot

## Description
A bot that speaks the truth of CJ and all creation.

## Requirements
Download the project and make sure you have [nodejs](https://nodejs.org/en/) and npm installed with discord.js inside the project folder (`npm install discord.js`).

## Usage
Open a terminal and go to the project location. To run the bot, type `node bot.js`.

